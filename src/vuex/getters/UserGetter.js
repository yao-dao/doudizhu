import userStore from '../stores/UserStore'
export function getZhuce(vuea){
	//默认传入所在的vue对象
	//alert(note.state.name);
	alert('loginbean值发生变化='+userStore.state.loginbean)
	return note.state.loginbean;
}
export function getLogin(vuea){
	//默认传入所在的vue对象
	//alert(note.state.name);
	alert('loginbean值发生变化='+userStore.state.loginbean)
	return note.state.loginbean;
}